# Changelog

## 0.1.0 — Unreleased

- Bootstrap canonical schema `1.0.0`, collectors, dedupe, routing evidence, semantic-version gates, isolated lab, synthetic fixtures, contract tests, packaging, and CI.
- Exclude downstream-only source kinds and all tmux implementation from the reusable core.
- Classify Agent `tool_response.resolvedModel` as version-sensitive local implementation evidence with an explicit non-attestation warning.
- Add a deterministic full-record tie-breaker for equal-score usage deduplication.
- Explicitly define the stable top-level API, including `CanonicalRecord`, `stable_id`, `dedupe_usage`, `DarioSeizer`, and `RoutingAccumulator`.
- Restore the complete lab CLI command surface while keeping network operations opt-in.
- Audit the public tree, wheel, sdist, and single-commit feature history for privacy and package-boundary regressions.
- Block release pending a maintainer-selected license.
